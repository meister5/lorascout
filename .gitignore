# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# host test binaries
build/

# local toolchains / virtualenvs
.venv/
venv/

# build artefacts
*.bin
*.elf
*.map

.DS_Store
.vscode/

# ...except the released images kept at the repo root, so M5Launcher's
# OTA > Favorites can point straight at a raw URL.
!/*.bin
